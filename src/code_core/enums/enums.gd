extends Node

class_name Enums

enum speed_level {
	Idle = 0,
	Slow = 100,
	Normal = 200,
	Fast = 300,
	VeryFast = 400
}

enum dragoon_type {
	Egg,
	Neck,
	Ball,
	Chonky
}

enum distance_level {
	Close = 5,
	Near = 10,
	Mid = 15,
	Far = 20,
	Distant = 25,
}

enum hit_zone {
	Head,
	Body,
}

enum ground_levels {
	ForeGround = 950,
	MidGround = 900,
	BackGround = 850,
}

enum fly_zone_lane {
	Hover = 750,
	Jump = 700,
	HighRise = 400,
	Fly = 250,
}
