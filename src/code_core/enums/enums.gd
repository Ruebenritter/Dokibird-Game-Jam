extends Node

class_name Enums

enum speed_level {
	Idle = 0,
	Slow = 100,
	Normal = 200,
	Fast = 300,
	VeryFast = 400
}

enum dragoon_type {
	Egg,
	Neck,
	Ball,
	Chonky
}

enum distance_level {
	close = 5,
	near = 10,
	middle = 15,
	far = 20,
	distant = 25,
}

enum hit_zone {
	Head,
	Body,
}
